# --- Core (필수, 프로토타입 실행) ---
openai>=1.40.0
langgraph>=0.2.0
langchain-core>=0.3.0
pypdf>=4.2.0
rank-bm25>=0.2.2
numpy>=1.26.0
python-dotenv>=1.0.0
requests>=2.31.0
boto3>=1.34.0

# --- 로컬 전환 (DGX Spark) 필수 ---
sentence-transformers>=3.0.0   # BGE-M3 dense 임베딩 (EMBED_BACKEND=local)
faster-whisper>=1.0.0          # 로컬 STT (음성 전사)
pillow>=10.0.0                 # 이미지 처리(VLM/OCR)
pypdfium2>=4.0.0               # PDF 페이지 렌더 → VLM OCR
olefile>=0.47                  # HWP(올레) 파싱 fallback
pymilvus>=2.4.0                # Milvus 벡터 DB
psycopg2-binary>=2.9.0         # PostgreSQL 백엔드

# --- Optional (있으면 기능 확장) ---
# surya-ocr>=0.6.0             # OCR_BACKEND=surya (ARM 친화)
# paddleocr>=2.7.0             # OCR_BACKEND=paddleocr (+paddlepaddle, aarch64 주의)
# FlagEmbedding>=1.2.0         # BGE-M3 dense+sparse 동시 추출
# pdfplumber>=0.11.0           # PDF 표 추출 품질 향상
